info:
  name: Liste des modules (admin)
  type: http
  seq: 7

http:
  method: GET
  url: "{{apiUrl}}/modules"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
