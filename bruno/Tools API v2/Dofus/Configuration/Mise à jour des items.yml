info:
  name: Mise à jour des items
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiUrl}}dofus/configuration/update-items?token={{token}}"
  params:
    - name: token
      value: "{{token}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
