info:
  name: Protected
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiJavaUrl}}/test/protected"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
