info:
  name: GET - Todos d'une liste
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiUrl}}todolist/todo/{{idtodolist}}/get"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
