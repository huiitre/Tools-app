info:
  name: Admin - Batch Delete Feedbacks
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{apiJavaUrl}}/feedbacks/admin"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "ids": [1, 2]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
