info:
  name: Ajouter un set
  type: http
  seq: 1

http:
  method: POST
  url: "{{apiJavaUrl}}dofus/set/add"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
