info:
  name: Admin - Update read status
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{apiUrl}}/feedbacks/admin/read-status"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "ids": [5],
        "isRead": true
      }
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "204 sans corps. Rôle minimum : ADMIN."
