info:
  name: Get all todolists
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiUrl}}todolist/get"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
