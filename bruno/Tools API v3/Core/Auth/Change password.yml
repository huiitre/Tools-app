info:
  name: Change password
  type: http
  seq: 12

http:
  method: PATCH
  url: "{{apiJavaUrl}}/user/password"
  body:
    type: json
    data: |-
      {
          "password": "test123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
