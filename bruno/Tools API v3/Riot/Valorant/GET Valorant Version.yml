info:
  name: GET Valorant Version
  type: http
  seq: 9

http:
  method: GET
  url: "{{apiUrl}}/riot/valorant/version"
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
