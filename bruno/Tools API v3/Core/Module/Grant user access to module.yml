info:
  name: Grant user access to module
  type: http
  seq: 4

http:
  method: POST
  url: "{{apiJavaUrl}}/modules/{{moduleId}}/users/{{userId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
