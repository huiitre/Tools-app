info:
  name: Electron session
  type: http
  seq: 4

http:
  method: POST
  url: "{{apiUrl}}/auth/electron/session"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
