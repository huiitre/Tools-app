info:
  name: Tier list
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiUrl}}/palworld/tierlist"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
