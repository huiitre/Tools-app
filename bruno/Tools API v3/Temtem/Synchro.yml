info:
  name: Synchro
  type: http
  seq: 1

http:
  method: POST
  url: "{{apiUrl}}/temtem/sync"
  body:
    type: text
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
