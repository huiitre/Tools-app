info:
  name: Recherche items
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiUrl}}/dofus/workshops/{{workshopId}}/items/search?query=padgref"
  params:
    - name: query
      value: padgref
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
