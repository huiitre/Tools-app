info:
  name: 2. Get Google Auth URL (electron)
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiUrl}}/auth/google/url?source=electron"
  auth:
    type: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
