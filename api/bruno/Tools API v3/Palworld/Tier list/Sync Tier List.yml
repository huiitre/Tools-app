info:
  name: Sync Tier List
  type: http
  seq: 2

http:
  method: POST
  url: "{{apiUrl}}/palworld/tierlist/admin/sync"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
