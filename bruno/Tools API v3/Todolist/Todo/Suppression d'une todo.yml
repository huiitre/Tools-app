info:
  name: Suppression d'une todo
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{apiUrl}}/todolists/{{todolistId}}/todos/{{todoId}}"
  body:
    type: text
    data: "{\r

      \    \"name\": \"Test todolist 200\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
