info:
  name: Liste des versions du jeux
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiUrl}}/dofus/game-versions"
  body:
    type: text
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
