info:
  name: List all skins
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiUrl}}/riot/valorant/skins?accountId=1"
  params:
    - name: accountId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
