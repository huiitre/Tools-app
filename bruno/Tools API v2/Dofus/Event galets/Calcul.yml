info:
  name: Calcul
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiUrl}}dofus/item/event-galet/calculate"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
