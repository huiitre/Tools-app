info:
  name: Delete expedition
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{apiJavaUrl}}/elite-dangerous/r2r/expeditions/{{expedition_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
