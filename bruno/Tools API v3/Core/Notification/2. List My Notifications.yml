info:
  name: 2. List My Notifications
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiUrl}}/notifications"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Récupère l'historique des notifications non supprimées.
