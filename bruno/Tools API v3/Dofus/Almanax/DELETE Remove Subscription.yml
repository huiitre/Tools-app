info:
  name: DELETE Remove Subscription
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{apiUrl}}/dofus/almanax/subscriptions/1"
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
