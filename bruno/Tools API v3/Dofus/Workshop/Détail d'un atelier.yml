info:
  name: Détail d'un atelier
  type: http
  seq: 8

http:
  method: GET
  url: "{{apiJavaUrl}}/dofus/workshops/{{workshopId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
