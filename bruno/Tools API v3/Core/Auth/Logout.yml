info:
  name: Logout
  type: http
  seq: 5

http:
  method: POST
  url: "{{apiUrl}}/auth/logout"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.deleteVar('token');

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
