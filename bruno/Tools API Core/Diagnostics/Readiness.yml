info:
  name: Readiness
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiCoreUrl}}/health/ready"
  auth:
    type: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
