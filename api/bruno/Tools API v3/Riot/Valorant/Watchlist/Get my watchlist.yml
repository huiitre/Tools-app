info:
  name: Get my watchlist
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiUrl}}/riot/valorant/watchlist"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
