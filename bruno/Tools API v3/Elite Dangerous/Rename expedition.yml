info:
  name: Rename expedition
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{apiUrl}}/elite-dangerous/r2r/expeditions/{{expedition_id}}/name"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Mon expédition Sagittarius A*"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
