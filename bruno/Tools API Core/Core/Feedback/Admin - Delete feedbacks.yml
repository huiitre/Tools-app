info:
  name: Admin - Delete feedbacks
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{apiUrl}}/feedbacks/admin"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "feedbackIds": [5]
      }
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "204 sans corps. Rôle minimum : ADMIN."
