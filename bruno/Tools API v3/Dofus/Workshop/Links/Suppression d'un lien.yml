info:
  name: Suppression d'un lien
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{apiJavaUrl}}/dofus/workshops/{{workshopId}}/links/{{linkId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
