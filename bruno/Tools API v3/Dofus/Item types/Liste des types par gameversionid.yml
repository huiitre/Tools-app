info:
  name: Liste des types par gameversionid
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiJavaUrl}}/dofus/item-types"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
