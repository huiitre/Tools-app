info:
  name: Liveness
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiUrl}}/health/live"
  auth:
    type: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
