info:
  name: Refresh
  type: http
  seq: 3

http:
  method: POST
  url: "{{apiUrl}}/auth/refresh"

runtime:
  scripts:
    - type: after-response
      code: |-
        const json = res.getBody();
        bru.setEnvVar('token', json.accessToken, { persist: false });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
