info:
  name: Skin par asset id
  type: http
  seq: 7

http:
  method: GET
  url: "{{apiUrl}}/riot/valorant/skins/by-asset/00000000-0000-0000-0000-000000000000?accountId=1"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Recherche par l'UUID Riot du skin racine.

  Rôle minimum : READ_ONLY.
