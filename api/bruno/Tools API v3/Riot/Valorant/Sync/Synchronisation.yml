info:
  name: Synchronisation
  type: http
  seq: 1

http:
  method: POST
  url: "{{apiUrl}}/riot/valorant/sync"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
