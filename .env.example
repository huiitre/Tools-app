# Accès SSH au NAS. Utilisé par scripts/ (sync assets/snapshots, clone de base) et par
# dev-console/server.js, qui ouvre le tunnel vers les serveurs de jeu (cf. TUNNELS).
SSH_HOST=ip-server
SSH_USER=username
SSH_PORT=port
