info:
  name: List accounts
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiUrl}}/riot/valorant/accounts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Liste les comptes Valorant liés au compte Tools courant.
