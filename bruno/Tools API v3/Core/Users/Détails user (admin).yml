info:
  name: Détails user (admin)
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiJavaUrl}}/users/{{userId}}"
  body:
    type: text
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
