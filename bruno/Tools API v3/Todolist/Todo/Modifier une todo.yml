info:
  name: Modifier une todo
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{apiJavaUrl}}/todolists/{{todolistId}}/todos/{{todoId}}"
  body:
    type: json
    data: "{\r\n    \"name\": \"audrey\"\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
