info:
  name: Delete module
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{apiJavaUrl}}/modules/{{moduleId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
