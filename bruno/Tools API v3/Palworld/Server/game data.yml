info:
  name: game data
  type: http
  seq: 12

http:
  method: GET
  url: "{{apiJavaUrl}}/palworld/server/game-data"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
