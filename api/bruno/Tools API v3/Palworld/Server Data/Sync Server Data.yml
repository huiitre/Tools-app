info:
  name: Sync Server Data
  type: http
  seq: 1

http:
  method: POST
  url: "{{apiUrl}}/palworld/server-data/admin/sync"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
