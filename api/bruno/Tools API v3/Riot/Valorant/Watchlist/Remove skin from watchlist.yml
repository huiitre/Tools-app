info:
  name: Remove skin from watchlist
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{apiUrl}}/riot/valorant/watchlist/:skinId"
  params:
    - name: skinId
      value: "1"
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
