info:
  name: Mise à jour des types d'item
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiJavaUrl}}dofus/configuration/update-item-types?token={{token}}"
  params:
    - name: token
      value: "{{token}}"
      type: query
  body:
    type: text
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
