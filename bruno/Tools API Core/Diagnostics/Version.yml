info:
  name: Version
  type: http
  seq: 4

http:
  method: GET
  url: "{{apiCoreUrl}}/version"
  auth:
    type: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
