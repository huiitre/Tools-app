info:
  name: Détail d'un skin
  type: http
  seq: 6

http:
  method: GET
  url: "{{apiUrl}}/riot/valorant/skins/1?accountId=1"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Skin inconnu : 404 `VALORANT_SKIN_NOT_FOUND`.

  Rôle minimum : READ_ONLY.
