info:
  name: Admin - Batch Update Read Status
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{apiJavaUrl}}/feedbacks/admin/read-status"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "ids": [1, 2],
          "isRead": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
