info:
  name: Liste des modules par user (admin)
  type: http
  seq: 8

http:
  method: GET
  url: "{{apiJavaUrl}}/modules/users/{{userId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
