info:
  name: Suppression d'un atelier
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{apiJavaUrl}}/dofus/workshops/{{workshopId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
