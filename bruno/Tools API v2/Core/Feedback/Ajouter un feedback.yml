info:
  name: Ajouter un feedback
  type: http
  seq: 1

http:
  method: POST
  url: "{{apiJavaUrl}}core/feedback/add"
  body:
    type: json
    data: "{\r\n    \"message\": \"test 1\"\r\n}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
