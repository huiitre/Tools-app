info:
  name: Détail d'une arme
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiUrl}}/riot/valorant/weapons/1"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Arme inconnue : 404 `VALORANT_WEAPON_NOT_FOUND`.

  Rôle minimum : READ_ONLY.
