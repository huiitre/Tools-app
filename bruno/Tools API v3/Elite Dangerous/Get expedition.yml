info:
  name: Get expedition
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiUrl}}/elite-dangerous/r2r/expeditions/{{expedition_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
