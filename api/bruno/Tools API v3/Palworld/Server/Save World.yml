info:
  name: Save World
  type: http
  seq: 7

http:
  method: POST
  url: "{{apiUrl}}/palworld/server/save"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
