info:
  name: Get Server Inventory (including passive IDs)
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiUrl}}/palworld/server-data/inventory"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
