info:
  name: Get Players
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiJavaUrl}}/palworld/server/players"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
