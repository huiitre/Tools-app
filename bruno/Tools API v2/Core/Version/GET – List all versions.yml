info:
  name: GET – List all versions
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiUrl}}core/version"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
