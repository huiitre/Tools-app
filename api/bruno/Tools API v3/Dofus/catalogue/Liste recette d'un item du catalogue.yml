info:
  name: Liste recette d'un item du catalogue
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiUrl}}/dofus/catalogue/item/{{itemId}}/recipe"
  body:
    type: text
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
