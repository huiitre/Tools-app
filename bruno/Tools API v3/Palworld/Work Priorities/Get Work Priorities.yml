info:
  name: Get Work Priorities
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiUrl}}/palworld/work-priorities"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
