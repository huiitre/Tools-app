info:
  name: Supprimer un tag d'un atelier
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{apiJavaUrl}}/dofus/workshops/{{workshopId}}/tags/{{workshopTagId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
