info:
  name: Suppression d'un tag
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{apiUrl}}/dofus/workshop/tags/{{workshopTagId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
