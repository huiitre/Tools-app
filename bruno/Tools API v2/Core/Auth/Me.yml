info:
  name: Me
  type: http
  seq: 6

http:
  method: GET
  url: "{{apiJavaUrl}}core/auth/me"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
