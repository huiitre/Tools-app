info:
  name: Get Settings
  type: http
  seq: 4

http:
  method: GET
  url: "{{apiJavaUrl}}/palworld/server/settings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
