info:
  name: supprimer un set
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{apiUrl}}dofus/set/81/delete"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
