info:
  name: Send mail
  type: http
  seq: 1

http:
  method: POST
  url: "{{apiUrl}}/mail"
  body:
    type: json
    data: |-
      {
        "to": ["yanisdeluca69@gmail.com"],
        "subject": "Test API Core",
        "text": "Envoi de test depuis Bruno via l'API Core."
      }
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
