info:
  name: récupérer les sets de l'utilisateur
  type: http
  seq: 8

http:
  method: GET
  url: "{{apiUrl}}dofus/set/get"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
