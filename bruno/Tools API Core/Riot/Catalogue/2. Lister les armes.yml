info:
  name: Lister les armes
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiUrl}}/riot/valorant/weapons"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Catalogue des armes, trié par nom.

  Rôle minimum : READ_ONLY.
