info:
  name: récupérer un set par son IDSET
  type: http
  seq: 9

http:
  method: GET
  url: "{{apiUrl}}dofus/set/125/get"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
