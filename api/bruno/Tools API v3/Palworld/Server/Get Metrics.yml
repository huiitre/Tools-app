info:
  name: Get Metrics
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiUrl}}/palworld/server/metrics"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
