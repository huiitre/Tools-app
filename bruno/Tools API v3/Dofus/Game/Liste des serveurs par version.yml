info:
  name: Liste des serveurs par version
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiJavaUrl}}/dofus/game-servers"
  body:
    type: text
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
