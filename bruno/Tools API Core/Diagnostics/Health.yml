info:
  name: Health
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiCoreUrl}}/health"
  auth:
    type: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
