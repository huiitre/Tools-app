info:
  name: Ajout de tags à l'atelier
  type: http
  seq: 9

http:
  method: POST
  url: "{{apiJavaUrl}}/dofus/workshops/{{workshopId}}/tags"
  body:
    type: json
    data: "[7, 17, 28]"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
