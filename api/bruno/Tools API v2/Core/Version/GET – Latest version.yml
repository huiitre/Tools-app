info:
  name: GET – Latest version
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiUrl}}core/version/latest"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
