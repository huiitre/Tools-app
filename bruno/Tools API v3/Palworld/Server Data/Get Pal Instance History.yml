info:
  name: Get Pal Instance History
  type: http
  seq: 5

http:
  method: GET
  url: "{{apiUrl}}/palworld/server-data/pal-instances/{{instanceId}}/history"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
