info:
  name: Protected (example)
  type: http
  seq: 4

http:
  method: GET
  url: "{{apiJavaUrl}}/<route-protegee>"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
