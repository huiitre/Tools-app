info:
  name: Get Base Pals
  type: http
  seq: 4

http:
  method: GET
  url: "{{apiJavaUrl}}/palworld/server-data/bases/{{baseId}}/pals"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
