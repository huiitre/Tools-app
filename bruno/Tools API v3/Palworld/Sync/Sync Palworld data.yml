info:
  name: Sync Palworld data
  type: http
  seq: 1

http:
  method: POST
  url: "{{apiUrl}}/palworld/sync"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
