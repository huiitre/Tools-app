info:
  name: Liste des users par module
  type: http
  seq: 9

http:
  method: GET
  url: "{{apiJavaUrl}}/modules/{{moduleId}}/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
