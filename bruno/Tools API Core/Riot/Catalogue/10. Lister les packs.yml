info:
  name: Lister les packs
  type: http
  seq: 10

http:
  method: GET
  url: "{{apiUrl}}/riot/valorant/bundles"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Catalogue des packs, trié par nom.

  Rôle minimum : READ_ONLY.
