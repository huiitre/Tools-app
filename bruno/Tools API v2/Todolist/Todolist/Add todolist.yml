info:
  name: Add todolist
  type: http
  seq: 3

http:
  method: POST
  url: "{{apiJavaUrl}}todolist/add"
  headers:
    - name: Authorization
      value: Bearer {{token}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
