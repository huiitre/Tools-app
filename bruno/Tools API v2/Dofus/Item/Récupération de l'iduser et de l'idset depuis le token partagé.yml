info:
  name: Récupération de l'iduser et de l'idset depuis le token partagé
  type: http
  seq: 4

http:
  method: GET
  url: "{{apiJavaUrl}}dofus/set/shared/6fd4d833138d"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
