info:
  name: Export expedition
  type: http
  seq: 6

http:
  method: GET
  url: "{{apiUrl}}/elite-dangerous/r2r/expeditions/{{expedition_id}}/export"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
