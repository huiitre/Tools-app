info:
  name: Mise à jour des recettes
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiUrl}}dofus/configuration/update-recipes?token={{token}}"
  params:
    - name: token
      value: "{{token}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
