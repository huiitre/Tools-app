info:
  name: Refresh
  type: http
  seq: 10

http:
  method: GET
  url: "{{apiUrl}}/riot/valorant/refresh"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
