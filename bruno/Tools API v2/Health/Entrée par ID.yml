info:
  name: Entrée par ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiUrl}}health/weight-log/4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
