info:
  name: 2. Get Google Auth URL (electron)
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiJavaUrl}}/auth/google/url?source=electron"
  params:
    - name: source
      value: electron
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
