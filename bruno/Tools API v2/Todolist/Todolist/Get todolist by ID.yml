info:
  name: Get todolist by ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiUrl}}todolist/2/get"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
