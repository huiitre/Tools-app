info:
  name: Suppression d'un item de l'atelier
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{apiUrl}}/dofus/workshops/{{workshopId}}/items/{{workshopItemId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
