info:
  name: Stop Server
  type: http
  seq: 11

http:
  method: POST
  url: "{{apiJavaUrl}}/palworld/server/stop"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
